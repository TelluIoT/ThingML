import "sensorAPI.thingml"

thing TemperatureSensor includes SensorAPI
{
	statechart behavior init Ready {
		
		on entry
        do
		  device = 'new org.thingml.devices.PotentiometerDemo(-40, 130)' 
		  'register()'
		end
			
		state Ready {}
		
	}
}