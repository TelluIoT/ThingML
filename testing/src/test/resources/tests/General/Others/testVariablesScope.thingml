import "../../thingml.thingml"

thing TestVariables includes Test 
@test "x # abcde"
{
	property p : Char = 'a'
	property arr : Char[1] = {'c'}
    
    statechart TestPropertyPing init state0 {
    	
    	on entry Test!Out(p)
    	
    	state state0 {
    		
    		property p : Char = 'b'
    		
    		on entry Test!Out(p)
    		
			transition -> state1 
			event e : Test?In
			guard e.c == 'x'
			action do
				var p : Char = 'e'
				do
					var p : Char = 'd'
					for(var p : Char <- arr) do
						Test!Out(p)
					end
					Test!Out(p)
				end
				Test!Out(p)
			end
		}

    	state state1 {
    		internal event Test?Done
			action TestDone()	
		}
    }
}
