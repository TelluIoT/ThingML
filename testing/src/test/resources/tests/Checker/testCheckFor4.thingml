import "datatypes.thingml" from stl

thing TestCheckFor
{
	property arr : Integer
	
    statechart TestAbstractFunction init INIT {
    	state INIT {
    		on entry do
    			for(var v : Integer = 3 <- arr) do //v cannot have init expression
    				println v
    			end
    		end
    	}
    }
}